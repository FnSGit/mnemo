# pnpm configuration for mnemo monorepo

# Use hoisted node_modules for better compatibility with native modules
node-linker=hoisted

# Auto install peers
auto-install-peers=true

# Shamefully hoist for maximum compatibility
shamefully-hoist=true